# T11 — a command that needs sudo with a password (every run's account is a normal desktop user whose sudo asks)
@password workshop
@timeout 300
@stall 60
launch workshop
type install htop for me
waitturn
probe composer-echo
line /exit
waitshell 30
