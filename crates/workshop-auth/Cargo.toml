[package]
name = "workshop-auth"
version = "0.1.0"
edition.workspace = true
license = "Apache-2.0"
publish = false
description = "Workshop connection picker policy: connection classes, picker cards and rails, presence-only CLI detection. Never reads another app's credentials."

[dependencies]
# Home resolution for the config snippet paths shown in the picker.
xai-dirs = { path = "../codegen/xai-dirs" }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
